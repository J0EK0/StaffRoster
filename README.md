# StaffRoster

醫院護理單位月排班工具。這是一個純前端小工具，不需要安裝、不需要 build，下載後直接打開 `index.html` 就可以使用。

## 主要功能

- 月排班表：依月份顯示每日班別、週六日、國定假日與連假標示。
- 員工管理：新增、刪除、設定固定班與禁忌班，並可拖曳調整名單順序。
- 偏好設定：可先填請假或想上的班，之後由修班流程套用。
- 底稿設定：可分別設定平日、普通假日、國定/連假的底稿輪換。
- 產生底稿：先依人工式輪轉節奏鋪出初版班表。
- 下一步修班：套用請假與偏好，並嘗試用換班修正禁忌班、接續規則、連續上班、休例國配額與缺班。
- 快速填入：選定班別後連續點格子填入，清空模式可像橡皮擦清掉單格。
- 規則檢查：畫面會列出違規與缺班診斷。
- Excel 匯出：可匯出帶有班別著色的月班表。

## 快速開始

如果只是要使用，不需要安裝任何東西。

1. 下載這個資料夾，或請別人把整包檔案傳給你。
2. 如果拿到的是壓縮檔，先按右鍵解壓縮。
3. 打開資料夾，找到 `index.html`。
4. 對 `index.html` 點兩下，會用瀏覽器打開排班系統。
5. 照畫面上方的順序操作：
   `設定偏好` → `底稿` → `產生底稿` → `下一步修班` → `匯出 Excel`

## 給第一次使用的人

照下面順序做就可以，不用懂程式。

### 1. 打開排班表

打開 `index.html` 後，畫面上方會看到年份和月份。先確認是不是要排的那個月份。

如果月份不對，可以按月份旁邊的左右箭頭，或直接選年份、月份。

### 2. 先檢查人員名單

左邊第一欄是人員姓名。

- 如果名字順序不對，可以用滑鼠按住名字拖曳調整。
- 如果有人固定上白班、PAR、門診，可以點名字旁邊的設定按鈕調整。
- 如果有人不能上 `N` 或 `E`，也可以在設定裡勾禁忌班。

### 3. 填請假或想上的班

按上方的 `設定偏好`。

接著在表格中點某個人的某一天，選他那天要請假或想上的班。

常見用法：

- 要請假：選 `請`
- 想指定某班：選 `N`、`E`、`7`、`中` 等班別
- 填錯了：再點一次那格，改成正確的班，或選清空

這些設定會先記起來，等你按 `下一步修班` 時才會正式套進班表。

### 4. 設定底稿

按上方的 `底稿`。

裡面分成三塊：

- `平日`：星期一到星期五的底稿
- `普通假日`：一般週六、週日
- `國定/連假`：國定假日和連假顯示日

如果你不想手動排，可以直接按裡面的 `產生底稿`，系統會照輪轉順序幫你填。

如果某個人不要參加某一區輪換，可以點他的名字，淡掉就代表這一區不使用他。三個區塊互不影響。

### 5. 產生底稿

回到主畫面後，按上方第 2 步 `產生底稿`。

這一步只是先排出一份初稿，所以畫面上如果有一些紅字或違規，不一定代表錯了。下一步會再修。

### 6. 下一步修班

按上方第 3 步 `下一步修班`。

系統會開始處理：

- 請假
- 禁忌班
- 大夜小夜前後接續
- 連續上班太多天
- 休、例、國配額
- 每天缺少的特殊班

如果修完還有問題，右邊或上方的規則提示會列出原因。這時再人工看那些紅字就好。

### 7. 手動微調

如果你想自己改某一格，直接點那一格選班別即可。

也可以用上方的快速填入工具列：

1. 先點要填的班別，例如 `N`。
2. 接著點表格格子，那些格子都會變成 `N`。
3. 如果要停止，就再點一次工具列按鈕，或改點其他班別。
4. `清空` 像橡皮擦，只會清掉你點的那一格。

### 8. 匯出 Excel

確認班表差不多後，按第 4 步 `匯出 Excel`。

瀏覽器會下載一個 Excel 檔，可以再拿去列印、傳 Line、或寄給同事。

### 常見狀況

- 打不開：請確認是點 `index.html`，不是點整個資料夾。
- 資料不見：資料存在同一台電腦的同一個瀏覽器，換瀏覽器或清除網站資料後就會看不到。
- 想重排：可以按更多選單裡的清空排班，或重新按 `產生底稿`。
- 紅字太多：先按 `下一步修班`，修完再看剩下的紅字。
- Excel 沒有匯出：請確認瀏覽器沒有擋下載，或看一下下載資料夾。

## 在 VSCode 上打開

如果你是用 VSCode 看這個專案，可以這樣開：

1. 打開 VSCode。
2. 點左上角 `File` → `Open Folder...`。
3. 選這個 `StaffRoster` 資料夾。
4. 打開左邊檔案清單裡的 `index.html`。

最簡單的使用方式，仍然是直接到資料夾裡點兩下 `index.html`。

如果想在 VSCode 裡用瀏覽器測試，有兩種方法。

方法一：用 VSCode 終端機

1. 在 VSCode 上方選 `Terminal` → `New Terminal`。
2. 在下面跳出的終端機輸入：

```bash
python3 -m http.server 8765
```

3. 打開瀏覽器，輸入：

```text
http://localhost:8765
```

方法二：用 Live Server 外掛

1. 在 VSCode 左邊點 Extensions。
2. 搜尋 `Live Server` 並安裝。
3. 回到 `index.html`。
4. 按右鍵，選 `Open with Live Server`。

如果只是要排班，不用管這段，直接點兩下 `index.html` 就好。

## 資料儲存

所有資料都存在瀏覽器的 `localStorage`，沒有後端、沒有資料庫，也不會自動上傳到任何地方。

也因為這樣：

- 同一台電腦、同一個瀏覽器才會看到同一份資料。
- 清除瀏覽器網站資料後，排班資料也會被清掉。
- 換電腦或換瀏覽器時，需要重新設定。

## 班別代碼

工作班別：

`N`、`E`、`3`、`7`、`1`、`2`、`中`、`△`、`A`、`R`、`門`、`白`

休假/不上班：

`休`、`休*`、`例`、`國`、`請`

`休*` 代表週日需要的「休 + 加班星號」名額，不是獨立休假配額；配額上仍算進 `休`。

## 排班規則摘要

- `N` 前一天必須是休假或 `N`。
- `N` 後一天不可接 `△`。
- `E` 後一天只能接 `休/例/國/請/7/E`。
- `3` 後一天只能接 `休/例/國/請/7/3`。
- 非固定班員工連續上班不可超過 6 天。
- 每人每月：
  - `休 + 休*` 數量 = 該月週六數
  - `例` 數量 = 該月週日數
  - `國` 數量 = 該月真正國定假日數
- 平日不上班人數最多 4 人。
- 每日特殊班別需要補齊。
- `請` 只代表使用者手動請假，自動排班不會自行產生 `請`。

## 底稿與修班邏輯

系統分成兩步，而不是一次把所有事情做完。

`產生底稿` 會先鋪出接近人工排班節奏的初稿：

- 平日依固定 pattern 輪轉，沒指定的格子補 `白`。
- 週六、週日、國定假日不排 `白`，改用假日特殊班與 `休/例/國`。
- 底稿視窗中可手填平日、普通假日、國定/連假底稿。
- 普通假日與國定/連假的人員排序可以獨立調整，不影響主排班表順序。

`下一步修班` 會再套用請假/偏好，並嘗試修正：

- 禁忌班
- `N/E/3` 前後接續問題
- 連續上班超過 6 天
- `休/例/國` 配額
- 每日特殊班缺班

若特殊班缺人：

- 平日會優先找同日 `白` 補班。
- 假日沒有白班時，會嘗試把同日 `休/例/國` 的人升成缺的特殊班，再把他的 `休/例/國` 補回某個平日白班。
- 候選人排序會先看「該特殊班誰最少」，再看特殊班總數。

## 國定假日與連假

`HOLIDAYS` 只放真正國定假日，會影響 `國` 配額。

連假週六日放在顯示用資料中，會出現在底稿的「國定/連假」區，但仍依實際星期計算：

- 週六仍算 `休` 配額。
- 週日仍算 `例` 配額。
- 只有真正國定假日才算 `國` 配額。

## 開發資訊

專案是 Vanilla JS，沒有 npm、沒有打包流程。

主要檔案：

```text
index.html           入口
css/style.css        樣式與表格配色
js/data.js           班別、員工、國定假日、localStorage key
js/calendar.js       日期與月份工具
js/staff.js          員工 CRUD 與排序
js/constraints.js    偏好/請假資料
js/validator.js      規則檢查
js/scheduler.js      底稿與修班引擎
js/export.js         Excel 匯出
js/app.js            UI 整合
```

基本語法檢查：

```bash
node --check js/app.js
node --check js/scheduler.js
```

## 已知限制

- 目前不是全局最佳化求解器，而是接近人工修班的啟發式流程。
- 內建假日資料以目前專案需求為主，新增年份時請更新 `js/data.js`。
- Excel 著色匯出使用 `xlsx-js-style` CDN；若完全離線使用，核心排班可用，但匯出樣式可能需要改成本地載入。
- 多月份歷史版本瀏覽、列印最佳化與更完整的互動式換班建議尚未實作。
