# StaffRoster

醫院護理單位月排班工具，搭配 CP-SAT 最佳化自動修班。

## 下載與啟動

### Windows（一般使用者）

**第一次使用：**

1. 打開瀏覽器，到這個頁面的 **Releases** 頁面
   - 在頁面右側找到 `Releases`，或直接點畫面右側的 **Latest** 標籤
2. 在最新版本下方找到 `StaffRoster-windows.zip`，點擊下載
3. 下載完成後，打開「下載」資料夾，找到 `StaffRoster-windows.zip`
4. 對 `StaffRoster-windows.zip` 按右鍵 → **解壓縮全部** → 選一個你記得的位置（例如桌面）→ 按解壓縮
5. 打開解壓縮後的資料夾，找到 `StaffRoster.exe`，雙擊執行
6. 若出現 **「Windows 已保護您的電腦」** 的藍色警告視窗：
   - 點 **「其他資訊」**
   - 點 **「仍要執行」**
7. 黑色視窗出現後等待幾秒，瀏覽器會自動開啟排班系統

**之後每次使用：**

直接到解壓縮的資料夾，雙擊 `StaffRoster.exe` 即可。不需要重新下載。

> 關閉排班系統時，記得把黑色視窗也一起關掉。

### Mac（開發者）

```bash
cd StaffRoster
python launcher.py
```

瀏覽器自動開啟 `http://127.0.0.1:8000`。

---

## 使用流程

### 1. 確認月份

畫面上方確認年份和月份。按左右箭頭或直接選擇切換。

### 2. 設定人員

左邊第一欄是人員名單。

- 拖曳名字可調整順序
- 點名字旁的設定按鈕可設定固定班（白班、PAR、門診）或禁忌班

### 3. 填請假與偏好

按上方 `設定偏好`，在表格中點某人某天，選擇：

- `請`：請假
- `N`、`E`、`7` 等：指定想上的班
- 點已填的格子可修改或清空

這些設定等按下一步修班時才會套入班表。

### 4. 設定底稿

按上方 `底稿`，設定平日、普通假日、國定/連假的輪班順序。

若不需要調整，可直接跳過這步。

### 5. 產生底稿

按 `① 產生底稿`。

系統依輪轉節奏鋪出初稿。此時出現紅字是正常的，下一步會修正。

### 6. 自動修班

按 `② 下一步修班`。

CP-SAT 求解器會自動處理：

- 請假與偏好
- 禁忌班
- N/E/3 前後接續規則
- 連續上班上限
- 休、例、國配額
- 每日特殊班缺班

### 7. 手動微調

若修班後仍有問題，可直接點格子手動改班別。

上方快速填入工具：先點班別 → 再點格子連續填入；點 `清空` 可逐格刪除。

### 8. 匯出 Excel

確認班表後按 `匯出 Excel`，下載帶有班別著色的 Excel 檔。

---

## 常見問題

- **瀏覽器沒自動開啟**：手動打開 `http://127.0.0.1:8000`
- **資料不見了**：資料存在瀏覽器 localStorage，換瀏覽器或清除網站資料後會消失
- **紅字太多**：先按 `下一步修班`，修完再看剩下的紅字
- **Excel 沒下載**：確認瀏覽器沒有擋下載

---

## 開發資訊

**前端**：Vanilla JS，無 npm，無打包流程。

**後端**：Python FastAPI + Google OR-Tools CP-SAT。

```bash
# 啟動後端
cd server
pip install -r requirements.txt
uvicorn main:app --reload --port 8000
```

主要檔案：

```
index.html           入口
css/style.css        樣式
js/data.js           班別定義、假日資料
js/scheduler.js      底稿生成
js/validator.js      規則檢查
js/app.js            UI 整合
server/solver.py     CP-SAT 修班求解器
server/main.py       FastAPI server
```
